{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "cmake",  // Use the cmake task type
            "command": "build",  // CMake Tools command to run
            "targets": [
                "all"
            ],
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}